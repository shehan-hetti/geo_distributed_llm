---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-node1-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-node1-service
  template:
    metadata:
      labels:
        app: llm-node1-service
    spec:
      containers:
        - name: llm-node1-service
          image: shehanhetti/llm-node1:latest
          imagePullPolicy: Always # Change this if container is in a registry
          ports:
            - containerPort: 50052
          # resources:
          #   limits:
          #     memory: "512Mi"
          #     cpu: "500m"
          #   requests:
          #     memory: "256Mi"
          #     cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: llm-node1-service
spec:
  selector:
    app: llm-node1-service
  ports:
    - protocol: TCP
      port: 50052
      targetPort: 50052
  type: ClusterIP