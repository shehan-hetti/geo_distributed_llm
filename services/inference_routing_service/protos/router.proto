syntax = "proto3";

package router;

message RouterRequest {
  string prompt = 1;
}

message RouterResponse {
  string result = 1;
}

service Router {
  rpc GetResult(RouterRequest) returns (RouterResponse) {}
}